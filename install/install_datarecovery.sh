#!/bin/bash

#
# This script installs data recovery tools that can be used to attempt
#  recovering data lost on (non deep) formatted drives. This script
#  doesn't do anything special but is a reminder of what software can
#  be tried.
#

sudo apt install testdisk
sudo apt install foremost
sudo apt install scalpel
