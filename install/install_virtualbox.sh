#
# Installs the latest virtualbox and guest additions from the multiverse repo
#

sudo add-apt-repository multiverse

sudo apt install virtualbox

sudo apt install virtualbox-guest-dkms virtualbox-guest-x11
